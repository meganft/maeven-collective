<body class="login-page">
  <div class="container extra-pad">
    <div class ="row">
      <div class = "text-center">
        <div class="col-sm-3">
        </div>
        <div class="col-sm-6">
          <div class="panel panel-default">
            <div class="panel-body less-padding">
              <h3 class="form-title">Create a New Tag </h3>
            </div>
            <div class="panel-footer panel-pad">
              <%= form_for [:admin, @tag], class: "form-group" do |f| %>
                <div class="form-group">
                  <%= f.text_field :name, class: "form-control login-items", placeholder: "Name", id: "Name" %>
                </div>
                <button type="submit" class="btn btn-default submit-button login-items" id="submitnewbutton">Create Tag</button></p>
              <% end %>
              </div>
            </div>
            <h3>Current Tags:</h3>
            <% @tags.each do |tag| %>
            <p><%= tag.name.titleize %> <%= link_to "Edit This Tag", edit_admin_tag_path(tag), class: "btn btn-info btn-sm" %> <%= link_to "Delete This Tag", admin_tag_path(tag), method: :delete, class: "btn btn-warning btn-sm",  data: { confirm: 'Are you sure you want to delete this tag?' } %></p>
            </p>
            <% end %>
            <p style ="padding-top:50px"><%= link_to " << Back to Your Dashboard", admin_dashboard_path, class: "btn btn-success btn-sm"%></p>
          </div>
          <div class="col-sm-3">
          </div>
        </div>
      </div>
    </body>
